import { defineConfig } from 'vite'

export default defineConfig({
    // Set your base URL, especially important for GitHub Pages deployments
    base: '/repo-junias',  // Add other configurations here as needed
    
    build: {
      outDir: 'dist', // Output directory for the build files
    }
  })
  